# Handoff matrix inputs

Generated by `python tools/matrix-audit/extract_handoffs.py`. Files are selected by matrix role from 10 local handoff archives. `manifest.json` records archive and file hashes. Run the extractor again instead of editing copied CSV cells manually. Raw multi-megabyte evidence matrices and nested source inventories remain in their original archives. Copied estimates are planning inputs, not verified implementation coverage.
