# Swift source status

The generated Swift `parser.c` is present at `../raw_parser_c/swift/parser.c` and is the authoritative source for this partition. The execution-ready tables are synchronized with its ABI data: `STATE_COUNT=10304`, `EXTERNAL_TOKEN_COUNT=34`, `lex_modes=10304`, `lex_accepts=1306`, and `parse_dispatch=263651`. Swift is therefore not classified as source-data-missing.
